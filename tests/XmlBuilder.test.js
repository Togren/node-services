
const XmlBuilder = require('XmlBuilder');

describe('XmlBuilder.fromJSON', () => {
    describe('given not a string or object', () => {
        test('')
    })
    describe('given a string', () => {
        test('that is invalid JSON throws error', () => {
            expect()
        })
        test('that is valid JSON returns XML string', () => {

        })
    })
    describe('given an object', () => {
        test('that is empty returns empty string', () => {

        })
        test('that is a valid JSON object returns XML string', () => {

        })
    })
})